import GuestHeroScene from '@/components/home/GuestHeroScene'

/** 비로그인 히어로 우측 일러스트 */
export default function HeroIllustration() {
  return (
    <GuestHeroScene className="h-auto w-full max-w-[360px]" />
  )
}
